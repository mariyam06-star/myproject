name: CI with Security Scan

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: SonarQube Scan
        uses: SonarSource/sonarcloud-github-action@master
        with:
          args: >
            -Dsonar.organization=mariyam06-star123
            -Dsonar.projectKey=a1692b079e015479e7551459da0daaf7ba5578e2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
